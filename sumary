Cau truc du lieu:

Session & Cookies:

TCP/UDP:

HTTP/HTTPS:

thuat toan tim kiem sap xep, do phuc tap mang bam, cay nhi phan:

primary type:
- valueOf use parsInt internal

List/ArrayList/LikList/Queue:

Con trỏ 32/64bit:
- const int *p; => con trỏ hằng => *p=100 crash
- int* const p; => hằng con trỏ => p=100 complier error

SQL/noSQL:

String pool:
- apply to same JVM
- create new object if substring and free huge string
- intern()

what is abstract class




Java multiThread:
- extends Thread => start(), main thread wait this thread terminate. Advantage: able to create temporary a thread and run
- A implements Runnable, Thread task = new Thread(A) => start(). Adv: able extends other class instead Thread
=> create Thread from Runnable => objects use same runnable => same resource.
setPriority() => 1-10 with 10 is most important

- ExecutorService
- Callable



* notify() => wake up first thread use wait()
* t1.join(2000) => wait thread t1 terminate or timeout 2s
